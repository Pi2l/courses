package org.m.courses.repository;

import org.m.courses.model.Course;
import org.m.courses.model.User;

import java.util.Optional;

public interface CourseRepository extends PrimaryRepository<Course, Long> {
}
